{
  "name": "lighthouse",
  "private": true,
  "version": "0.1.0",
  "description": "Conversational blockchain data analyst: ask a question in plain English, the backend calls typed data tools against stubbed on-chain datasets, and the answer renders as a professional chart.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "seed": "npm run seed --workspace server",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "test:server": "npm test --workspace server"
  },
  "engines": {
    "node": ">=18"
  }
}
