# Optional. Without this set, the server falls back to a deterministic
# keyword-based router (src/chat/fallbackRouter.js) so the app still runs
# end-to-end with no external dependency.
ANTHROPIC_API_KEY=

# Optional overrides.
PORT=8787
LIGHTHOUSE_MODEL=claude-sonnet-5
